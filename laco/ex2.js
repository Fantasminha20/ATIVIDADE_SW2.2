// while faca um programa que repita um numero de 1 a 30

export function numero() {
    let a = 0
    let lista = []

    while (a < 30) {
        a++
        lista.push(a)
    }

    return lista
};
